config(:friendly1){
  uri "sqlite://test.db"
  plug DbAgile::Plugin::AgileTable
  plug DbAgile::Plugin::AgileKeys
}
config(:friendly2){
  uri "sqlite://test.db"
  plug DbAgile::Plugin::AgileKeys, {:candidate => /id/}
  plug DbAgile::Plugin::Defaults, {:hello => "world"}
}
config(:friendly3){
  uri "sqlite://test.db"
  plug DbAgile::Plugin::Defaults, {:now => DbAgile::Plugin::Touch::now}
}
config(:friendly4){
  uri "sqlite://test.db"
  schema_file "schema.yaml"
  plug DbAgile::Plugin::Defaults, {:now => DbAgile::Plugin::Touch::now}
}
config(:friendly5){
  uri "sqlite://test.db"
  schema_files ["schema.yaml", "dbagile.yaml"]
  plug DbAgile::Plugin::Defaults, {:now => DbAgile::Plugin::Touch::now}
}
