start_transaction
  (key! :suppliers, [:'s#'])

  (assert "suppliers/s# is a key",
    (is_key? :suppliers, [:'s#']))

  (assert_false "suppliers/name is not a candidate key",
    (is_key? :suppliers, [:'name']))

  (assert "A super key is a key!",
    (is_key? :suppliers, [:'s#', :'name']))
commit