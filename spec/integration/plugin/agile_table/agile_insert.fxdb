(plug AgileTable)

(insert :tools, {:name => "agiledb"})
(assert "The tools table has been created on agile insert",
  (table_exists? :tools))

(assert "The tools table contains correct tuples",
  (equal? (to_a :tools), [ {:name => "agiledb"} ]))